# Build output — generated by `npm run build`, not source code
dist/

# Dependencies — reinstalled by `npm install` on every deploy
node_modules/

# Local environment files — never commit secrets
.env
.env.local
.env.*.local

# Editor / OS noise
.DS_Store
Thumbs.db
*.suo
.vscode/settings.json

# Test coverage output
coverage/
